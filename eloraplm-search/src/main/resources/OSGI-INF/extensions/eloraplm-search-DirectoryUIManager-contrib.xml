<?xml version="1.0"?>

<component name="com.aritu.eloraplm.search.DirectoryUIManager.contrib">

  <extension target="org.nuxeo.ecm.directory.ui.DirectoryUIManager" point="directories">

    <directory layout="vocabulary" sortField="ordering" name="eloras_facetType"/>
    <directory layout="vocabulary" sortField="label" name="eloras_docType"/>
    <directory layout="vocabulary" sortField="ordering" name="eloras_lifeCycle"/>
    
    <!--  CAD documents search related vocabularies -->
    <directory layout="vocabulary" sortField="ordering" name="eloras_cadDocuments_docType"/>
    <directory layout="vocabulary" sortField="ordering" name="eloras_cadDocuments_lifeCycle"/>
    
    <!--  Basic documents search related vocabularies -->
    <directory layout="vocabulary" sortField="ordering" name="eloras_basicDocuments_docType"/>
    <directory layout="vocabulary" sortField="ordering" name="eloras_basicDocuments_lifeCycle"/>
    
    <!--  BOMs search related vocabularies -->
    <directory layout="vocabulary" sortField="ordering" name="eloras_boms_lifeCycle"/>
    
    <!--  CM search related vocabularies -->
    <directory layout="vocabulary" sortField="ordering" name="eloras_ecos_lifeCycle"/>
    
    <!--  Projects search related vocabularies -->
    <directory layout="vocabulary" sortField="ordering" name="eloras_projects_lifeCycle"/>
    
    <!--  QM documents search related vocabularies -->
    <directory layout="vocabulary" sortField="ordering" name="eloras_qmDocuments_docType"/>
    <directory layout="vocabulary" sortField="ordering" name="eloras_qmDocuments_lifeCycle"/>
    
  </extension>

</component>
